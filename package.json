{
  "type": "module",
  "devDependencies": {
    "buffer": "^5.7.1",
    "concurrently": "^7.6.0",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "npx parcel index.html",
    "start": "concurrently npm:start:*"
  },
  "dependencies": {
    "axios": "^1.3.0"
  }
}
